[pytest]
testpaths = tests/suite_02_adapter_tests
markers =
    core: the baseline every dbt adapter is expected to pass
    extended: capability suites beyond the baseline
    athena_conversion: converted dbt-athena fixtures executed against dbt-dlc
addopts =
    -q
    -ra
    --tb=short
    --strict-markers
    --color=no
