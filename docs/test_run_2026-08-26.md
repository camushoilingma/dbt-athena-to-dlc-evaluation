# Exact execution manifest: 2026-08-26

This document identifies the exact command, source file, input, warehouse output,
and assertion used in the recorded run. It does not infer outputs that were not
queried or persisted.

## Entry point and target

The live command was:

```bash
./scripts/run_migration.sh full
```

The subcommand dispatch is at
[`run_migration.sh:105-108`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L105-L108).
The twelve live dbt commands are at
[`run_migration.sh:79-96`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L79-L96).

```text
dbt-core: 1.12.3
dbt-dlc:  1.1.1
catalog:  DataLakeCatalog
schema:   athena_migration
```

The catalog and schema come from
[`profiles.yml:17-32`](../projects/jaffle_shop_athena_migration/profiles.yml#L17-L32).
No Athena engine and no Kaggle dataset were used.

## Runner result files

[`run_migration.sh:39-58`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L39-L58)
runs each command, writes stdout/stderr to `results/<case>.log`, and appends the
exit code to `results/runtime_results.csv`.
[`run_migration.sh:75-77`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L75-L77)
creates the CSV:

```csv
case,expectation,exit_code,outcome,log
```

The recorded files are local under
`projects/jaffle_shop_athena_migration/results/live_20260826/` and are ignored by
Git.

## 1. Seed inputs and outputs

Exact command at
[`run_migration.sh:79`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L79):

```bash
dbt seed --target dev --full-refresh
```

All outputs are Iceberg tables because of
[`dbt_project.yml:41-43`](../projects/jaffle_shop_athena_migration/dbt_project.yml#L41-L43).

| Exact input file | Rows | Exact persistent output |
|---|---:|---|
| [`seeds/raw_customers.csv`](../projects/jaffle_shop_athena_migration/seeds/raw_customers.csv) | 100 | table `DataLakeCatalog.athena_migration.raw_customers` |
| [`seeds/raw_orders.csv`](../projects/jaffle_shop_athena_migration/seeds/raw_orders.csv) | 99 | table `DataLakeCatalog.athena_migration.raw_orders` |
| [`seeds/raw_payments.csv`](../projects/jaffle_shop_athena_migration/seeds/raw_payments.csv) | 113 | table `DataLakeCatalog.athena_migration.raw_payments` |
| [`seeds/raw_athena_events.csv:2-4`](../projects/jaffle_shop_athena_migration/seeds/raw_athena_events.csv#L2-L4) | 3 | table `DataLakeCatalog.athena_migration.raw_athena_events` |
| [`seeds/raw_order_updates.csv:2-5`](../projects/jaffle_shop_athena_migration/seeds/raw_order_updates.csv#L2-L5) | 4 | table `DataLakeCatalog.athena_migration.raw_order_updates` |
| [`seeds/expected_order_upserts.csv:2-4`](../projects/jaffle_shop_athena_migration/seeds/expected_order_upserts.csv#L2-L4) | 3 | table `DataLakeCatalog.athena_migration.expected_order_upserts` |

Observed result: six seeds passed.

## 2. Jaffle baseline inputs and outputs

Exact command at
[`run_migration.sh:80`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L80):

```bash
dbt build --target dev --select tag:jaffle_baseline
```

The materializations and tag are defined at
[`dbt_project.yml:24-32`](../projects/jaffle_shop_athena_migration/dbt_project.yml#L24-L32).

| SQL file executed | Exact relation input | Exact persistent output |
|---|---|---|
| [`stg_customers.sql`](../projects/jaffle_shop_athena_migration/models/staging/stg_customers.sql) | table `DataLakeCatalog.athena_migration.raw_customers` | view `DataLakeCatalog.athena_migration.stg_customers` |
| [`stg_orders.sql`](../projects/jaffle_shop_athena_migration/models/staging/stg_orders.sql) | table `DataLakeCatalog.athena_migration.raw_orders` | view `DataLakeCatalog.athena_migration.stg_orders` |
| [`stg_payments.sql`](../projects/jaffle_shop_athena_migration/models/staging/stg_payments.sql) | table `DataLakeCatalog.athena_migration.raw_payments` | view `DataLakeCatalog.athena_migration.stg_payments` |
| [`customers.sql`](../projects/jaffle_shop_athena_migration/models/customers.sql) | views `stg_customers`, `stg_orders`, `stg_payments` | table `DataLakeCatalog.athena_migration.customers` |
| [`orders.sql`](../projects/jaffle_shop_athena_migration/models/orders.sql) | views `stg_orders`, `stg_payments` | table `DataLakeCatalog.athena_migration.orders` |

The twenty generic tests are declared in
[`models/staging/schema.yml:4-31`](../projects/jaffle_shop_athena_migration/models/staging/schema.yml#L4-L31)
and
[`models/schema.yml:4-82`](../projects/jaffle_shop_athena_migration/models/schema.yml#L4-L82).
Tests produced no persistent object; each passed when its query returned zero
invalid rows.

Observed result: three views, two tables, and twenty tests passed.

## 3. Namespace probe input and output

Exact command at
[`run_migration.sh:81`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L81):

```bash
dbt run --target dev --select tag:namespace_probe
```

SQL executed:
[`namespace_probe.sql:3-5`](../projects/jaffle_shop_athena_migration/models/migration/namespace_probe.sql#L3-L5)

```text
input:  table DataLakeCatalog.athena_migration.raw_orders
output: view  DataLakeCatalog.athena_migration.namespace_probe
```

The view contains `count(*) as order_count`. No separate test asserted that the
count was 99. Observed result: one view passed.

## 4. Original Athena expression attempts

All four commands read table
`DataLakeCatalog.athena_migration.raw_athena_events`, created from
[`raw_athena_events.csv:2-4`](../projects/jaffle_shop_athena_migration/seeds/raw_athena_events.csv#L2-L4).

| Command line | SQL file and line | Exact attempted output | Recorded result |
|---|---|---|---|
| [`run_migration.sh:83`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L83) | [`athena_from_iso8601.sql:3-4`](../projects/jaffle_shop_athena_migration/models/migration/athena_source/athena_from_iso8601.sql#L3-L4) | view `DataLakeCatalog.athena_migration.athena_from_iso8601` | not created; `from_iso8601_timestamp` unresolved |
| [`run_migration.sh:84`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L84) | [`athena_date_parse.sql:3-4`](../projects/jaffle_shop_athena_migration/models/migration/athena_source/athena_date_parse.sql#L3-L4) | view `DataLakeCatalog.athena_migration.athena_date_parse` | not created; `try` unresolved |
| [`run_migration.sh:85`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L85) | [`athena_date_parse_only.sql:3-4`](../projects/jaffle_shop_athena_migration/models/migration/athena_source/athena_date_parse_only.sql#L3-L4) | view `DataLakeCatalog.athena_migration.athena_date_parse_only` | not created; `date_parse` unresolved |
| [`run_migration.sh:86`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L86) | [`athena_json_extract.sql:3-4`](../projects/jaffle_shop_athena_migration/models/migration/athena_source/athena_json_extract.sql#L3-L4) | view `DataLakeCatalog.athena_migration.athena_json_extract` | not created; `json_extract_scalar` unresolved |

These were observation cases. Their nonzero exit codes were recorded without
making the complete runner fail.

## 5. Converted DLC expression inputs and outputs

Exact command at
[`run_migration.sh:88`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L88):

```bash
dbt build --target dev --select tag:dlc_converted
```

The first three models read table
`DataLakeCatalog.athena_migration.raw_athena_events`.

| SQL file and line | Exact expression | Exact persistent output |
|---|---|---|
| [`dlc_iso_timestamp.sql:3-4`](../projects/jaffle_shop_athena_migration/models/migration/dlc_converted/dlc_iso_timestamp.sql#L3-L4) | `to_timestamp(event_ts_iso)` | view `DataLakeCatalog.athena_migration.dlc_iso_timestamp` |
| [`dlc_date_parse.sql:3-4`](../projects/jaffle_shop_athena_migration/models/migration/dlc_converted/dlc_date_parse.sql#L3-L4) | `to_date(event_date_text, 'yyyy-MM-dd')` | view `DataLakeCatalog.athena_migration.dlc_date_parse` |
| [`dlc_json_extract.sql:3-4`](../projects/jaffle_shop_athena_migration/models/migration/dlc_converted/dlc_json_extract.sql#L3-L4) | `get_json_object(payload, '$.channel')` | view `DataLakeCatalog.athena_migration.dlc_json_extract` |
| [`dlc_pattern_results.sql:1-10`](../projects/jaffle_shop_athena_migration/models/migration/dlc_converted/dlc_pattern_results.sql#L1-L10) | joins the three views by `id` | view `DataLakeCatalog.athena_migration.dlc_pattern_results` |

Five generic tests are declared at
[`models/migration/schema.yml:4-13`](../projects/jaffle_shop_athena_migration/models/migration/schema.yml#L4-L13).
The sixth is
[`assert_dlc_pattern_values.sql:1-9`](../projects/jaffle_shop_athena_migration/tests/assert_dlc_pattern_values.sql#L1-L9):

```text
input:  view DataLakeCatalog.athena_migration.dlc_pattern_results
output: no persistent object; test query must return zero rows
checks: event_ts non-null, event_date non-null, channel web/mobile/store by ID
```

Observed result: four views and six tests passed. The singular test was executed
again by
[`run_migration.sh:89`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L89)
and passed. No command exported the result rows, so this evidence does not claim
exact timestamp or date display values.

## 6. Iceberg MERGE batch 1

Exact command at
[`run_migration.sh:91-92`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L91-L92):

```bash
dbt run --target dev --select iceberg_order_upserts \
  --full-refresh --vars '{load_batch: 1}'
```

Model executed:
[`iceberg_order_upserts.sql:1-19`](../projects/jaffle_shop_athena_migration/models/migration/iceberg_order_upserts.sql#L1-L19)

Exact input from
[`raw_order_updates.csv:2-3`](../projects/jaffle_shop_athena_migration/seeds/raw_order_updates.csv#L2-L3):

```csv
batch_id,order_id,customer_id,order_date,status,amount_cents
1,1,10,2024-01-01,placed,1000
1,2,20,2024-01-02,placed,2000
```

```text
output: Iceberg incremental table DataLakeCatalog.athena_migration.iceberg_order_upserts
```

`--full-refresh` created or replaced the table from batch 1. Observed result:
one incremental model passed.

## 7. Iceberg MERGE batch 2

Exact command at
[`run_migration.sh:93-94`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L93-L94):

```bash
dbt run --target dev --select iceberg_order_upserts \
  --vars '{load_batch: 2}'
```

Exact input from
[`raw_order_updates.csv:4-5`](../projects/jaffle_shop_athena_migration/seeds/raw_order_updates.csv#L4-L5):

```csv
batch_id,order_id,customer_id,order_date,status,amount_cents
2,1,10,2024-01-01,shipped,1200
2,3,30,2024-01-03,placed,3000
```

```text
output updated in place: Iceberg incremental table DataLakeCatalog.athena_migration.iceberg_order_upserts
```

[`iceberg_order_upserts.sql:2-8`](../projects/jaffle_shop_athena_migration/models/migration/iceberg_order_upserts.sql#L2-L8)
sets `incremental_strategy='merge'`, `unique_key='order_id'`, and
`partition_by='order_date'`. Observed result: one incremental model passed.

## 8. Exact MERGE reconciliation

Exact command at
[`run_migration.sh:95-96`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L95-L96):

```bash
dbt test --target dev \
  --select iceberg_order_upserts assert_iceberg_upsert_matches_expected
```

```text
actual input:   table DataLakeCatalog.athena_migration.iceberg_order_upserts
expected input: table DataLakeCatalog.athena_migration.expected_order_upserts
output:         no persistent object; test queries must return zero rows
```

[`assert_iceberg_upsert_matches_expected.sql:1-35`](../projects/jaffle_shop_athena_migration/tests/assert_iceberg_upsert_matches_expected.sql#L1-L35)
compares all five columns in both directions with `EXCEPT`. The exact expected
input is
[`expected_order_upserts.csv:2-4`](../projects/jaffle_shop_athena_migration/seeds/expected_order_upserts.csv#L2-L4):

```csv
order_id,customer_id,order_date,status,amount_cents
1,10,2024-01-01,shipped,1200
2,20,2024-01-02,placed,2000
3,30,2024-01-03,placed,3000
```

Four generic tests are declared at
[`models/migration/schema.yml:15-22`](../projects/jaffle_shop_athena_migration/models/migration/schema.yml#L15-L22):
unique `order_id` and non-null `order_id`, `customer_id`, and `order_date`.

Observed result: all five tests returned zero invalid rows and passed. The
two-way comparison proves that the final table contained exactly the three rows
above, with no missing or additional rows.

## Offline namespace CSV

The separate offline command was:

```bash
./scripts/run_migration.sh lint
```

[`run_migration.sh:63-66`](../projects/jaffle_shop_athena_migration/scripts/run_migration.sh#L63-L66)
runs `dbt parse`, runs `dbt compile` without introspection, and passes
`target/manifest.json` to
[`inspect_manifest.py:10-31`](../projects/jaffle_shop_athena_migration/scripts/inspect_manifest.py#L10-L31).

```text
input:  target/manifest.json
output: results/namespace_compile.csv
```

The output CSV contains
`unique_id,database,schema,identifier,namespace_levels,relation_name` for all 15
models and 6 seeds. It is a local generated file, not a warehouse relation.

## Persistent warehouse output inventory

Tables:

```text
DataLakeCatalog.athena_migration.customers
DataLakeCatalog.athena_migration.expected_order_upserts
DataLakeCatalog.athena_migration.iceberg_order_upserts
DataLakeCatalog.athena_migration.orders
DataLakeCatalog.athena_migration.raw_athena_events
DataLakeCatalog.athena_migration.raw_customers
DataLakeCatalog.athena_migration.raw_order_updates
DataLakeCatalog.athena_migration.raw_orders
DataLakeCatalog.athena_migration.raw_payments
```

Views:

```text
DataLakeCatalog.athena_migration.dlc_date_parse
DataLakeCatalog.athena_migration.dlc_iso_timestamp
DataLakeCatalog.athena_migration.dlc_json_extract
DataLakeCatalog.athena_migration.dlc_pattern_results
DataLakeCatalog.athena_migration.namespace_probe
DataLakeCatalog.athena_migration.stg_customers
DataLakeCatalog.athena_migration.stg_orders
DataLakeCatalog.athena_migration.stg_payments
```

The four `athena_*` views are absent because their create-view statements failed.
The 31 dbt data tests are queries and did not create persistent warehouse objects.
